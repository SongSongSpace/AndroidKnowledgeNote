**顺序存储**
- 空间地址连续。连续的空间。
- 当没有连续空间了，但还需要申请空间时，就会抛出内存错误。
- 是一种设计上的取舍。用连续空间换速度。
- 优势：随机访问快。时间复杂度O(1)。
- 劣势：删除元素、插入非尾部元素，需要移动大量元素。
- 扩展：
	- list。
	- 是一种线性表顺序存储结构。存储的是对象的内存地址。
	- 可以实现存放不同类型对象。
- **动态数组**是线性表顺序存储的代表数据结构。
