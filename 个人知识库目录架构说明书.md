本知识库采用 **“领域（一级） + 通用动作模板（二级） + 标签（横切维度）”** 的架构，核心逻辑是**领域与研发动作解耦**。请根据以下规则，帮助我建议新文档的归档路径。

## 一、 顶层一级领域（01–09）

知识库按垂直技术领域划分，外加工作流和软技能：

- `01_基础与系统`：Java/Android 并发、系统源码、网络基础、底层架构
- `02_UI与交互`：UI 控件、交互体验、动效、适配
- `03_网络与协议`：TCP/HTTP、局域网发现（SSDP/UPnP 等）
- `04_音视频技术`：音视频采集、编解码、RTC、点播直播、投屏
- `05_AI与算法`：数据结构、基础算法、大模型与 LLM、深度学习
- `06_工程与运维`：发布部署、测试与质量、运维监控
- `07_架构与设计`：设计模式、系统架构、重构与代码整洁
- `08_业务与需求`：需求分析、产品思维（预留）
- `09_经验与复盘`：个人成长、项目复盘、方法论
- `00_Inbox_收件箱`：临时中转站（不做分类，定期清空）

## 二、 标准领域动作模板（02、03 适用）

在 `02_UI与交互`、`03_网络与协议` 等常规领域下，采用以下固定二级目录：

- `01_核心原理`：底层机制、源码分析、概念解析
- `02_代码片段与实践`：业务落地代码、API 使用、最佳实践
- `03_踩坑与解决`：Bug 排查、异常处理、反模式规避
- `04_架构与设计`：模块设计、框架选型、解耦方案

## 三、 特殊领域的定制结构（04、05、06、07、09）

- `04_音视频技术`：按业务场景细分（`01_通用基础`、`02_RTC`、`03_点播与直播`、`04_投屏与多屏互动`），每个业务场景内部再复用“标准动作模板”。
- `05_AI与算法`：分为 `00_资源`、`01_核心原理`（内部再细分为数据结构、基础算法、大模型与LLM、深度学习）、`02_代码片段与实践`、`03_踩坑与解决`。
- `06_工程与运维`：分为 `01_发布与部署`、`02_测试与质量`、`03_运维与监控`。
- `07_架构与设计`：分为 `01_设计模式`、`02_系统架构`、`03_重构与代码整洁`。
- `09_经验与复盘`：分为 `01_个人成长`、`02_项目复盘`、`03_方法论`。

## ## 四、 强制文档命名规范（极其重要）

**格式：`领域-具体内容.md`** （如 `并发-Executor框架.md`、`投屏-Airplay编解码与传输协商.md`）。

- **必须有前缀**：前缀代表知识领域（如 `并发-`、`源码-`、`UI-`、`网络-`、`RTC-`、`算法-`、`LLM-`、`架构-`），全局搜索时防止重名。
- **拒绝宽泛词**：严禁使用《目标》、《基础》、《架构》、《学习笔记》这种无语境命名，必须具体化（如《架构-高级工程师架构能力模型与演进》）。
- **日期处理**：日常知识笔记不强制加日期。只有具有时间属性的工作日志或问题快照才保留 `YYMMDD-` 前缀。

## 五、 MOC（内容地图）机制

- 每个一级领域根目录下均有一个 `00_领域名_MOC.md`（如 `00_音视频_MOC.md`）。
- MOC 不是目录的复制，而是**逻辑路由**。它按主题串联高价值笔记，并预留 `⏳ 待填补 / 待学` 钩子。
- 新文档入库时，必须同步在对应的 MOC 中挂载 `[[双链]]`。

## 六、 标签应用规则（横切维度）

标签用于弥补目录“唯一物理存放位置”的局限，实现**交叉检索**。核心原则是**标签绝不重复目录名**（笔记已在 `04_音视频技术` 下，就不要再打 `#音视频`）。每篇笔记的标签控制在 **2–4 个**，且统一使用层级化命名（`#维度/值`）。

### 1. 四大固定维度

|维度|标签示例|用途|
|---|---|---|
|**状态**|`#状态/待整理` `#状态/草稿` `#状态/进行中` `#状态/已完成` `#状态/待验证`|替代 `待学`、`学习笔记` 等旧分类|
|**类型**|`#类型/源码分析` `#类型/API实践` `#类型/方案对比` `#类型/踩坑总结` `#类型/性能调优`|补充二级目录颗粒度|
|**技术栈**|`#技术栈/Android` `#技术栈/WebRTC` `#技术栈/APT` `#技术栈/LLM`|精准定位具体技术|
|**场景**|`#场景/投屏` `#场景/在线会议` `#场景/短视频` `#场景/大屏端`|将技术与实际业务关联|

### 2. 实操示例

- `RTC-WebRTC SDP详解.md` → `#状态/已完成` `#类型/源码分析` `#技术栈/WebRTC`
- `发布-GooglePlay上架流程.md` → `#状态/已完成` `#类型/API实践` `#场景/海外发布`
- `Android-数据库版本迁移.md` → `#状态/进行中` `#类型/方案对比` `#技术栈/APT` `#技术栈/Android`

### 3. 输入方式

推荐在正文头部使用 YAML Frontmatter：

```yaml
tags: [状态/已完成, 类型/源码分析, 技术栈/WebRTC]
```

也可在正文末尾行内添加，但优先推荐 Frontmatter 以保持正文整洁。

---

### 💡 复制以下 Prompt 直接发给协作者/AI：

> “以上是我的知识库架构说明书。后续我会向你发送一些散落的笔记名称或内容，请你根据【顶层一级领域】判断业务归属，根据【标准动作模板】判断内容属性（是原理、代码、踩坑还是架构），结合【强制命名规范】直接给我一个明确的最终路径和重命名建议（例如：`04_音视频技术/02_RTC/01_核心原理/RTC-WebRTC SDP详解`）。同时，按【六、标签应用规则】给出 2–4 个推荐标签，格式为 `#维度/值`。不需要解释太多分类逻辑，直接给路径和标签组合即可。”