**后台启动** **APP** **某个页面**

  

  

**1****、探索研究**

  

**A.** **相关权限**

a. display over other apps

b. display pop-up windows while running in the background

c. show on lock screen

**B.** **实现思路**

a. 先检查并请求相应必要权限

b. 保证行为时机接收信号在后台也能接收到

c. 接收到信号，intent 打开目标activity

**C.** **难点**

a. 检查不同机型需要的必要权限是否授予，确保尽可能大范围的应用

b. 在非用户主动杀死 APP 的情况下，保证 APP 尽可能的一直运行在后台

**D.** **背景**

a. 在 10 以下的机子中，不用请求权限，可以直接在后台启动 APP。但如果在Activity上下文之外启动Activity会有限制，需要给intent添加flag：FLAG_ACTIVITY_NEW_TASK。

b. 在 10以上的机子中，限制 App 从后台启动（[https://developer.android.com/guide/components/activities/background-starts](https://developer.android.com/guide/components/activities/background-starts)），需要额外请求display over other apps权限。而部份机型除了请求这个权限以外，还需要保证权限b也授予。

c. 若想要在设备处于锁屏状态下，依然可以在后台启动 APP，需要授予权限c。

d. 在系统内存资源不足的情况下，会优先杀掉低优先级的进程。用户可感知的 app往往不希望被系统所杀，系统默认service 是后台的，通过startForeground 把服务提到foreground 优先级，即成为可感知的级别。

  

  

保活

a. 背景：在系统内存资源不足的情况下，会优先杀掉低优先级的进程。用户可感知的 app往往不希望被系统所杀，系统默认service 是后台的，通过startForeground 把服务提到foreground 优先级，即成为可感知的级别。

b. 实现

  

  

**特殊点：**

- 1、从 Android Q 开始，SYSTEM_ALERT_WINDOW 系统弹窗在低内存设备上被禁止。 [https://blog.csdn.net/qq_37858386/article/details/125780024](https://blog.csdn.net/qq_37858386/article/details/125780024)

2、判断是否支持display over other apps  （Android O）在某些设备里有问题 [https://stackoverflow.com/questions/46173460/why-does-settings-candrawoverlays-method-in-android-8-returns-false-when-use](https://stackoverflow.com/questions/46173460/why-does-settings-candrawoverlays-method-in-android-8-returns-false-when-use)

  

退出 APP 并在后台运行： moveTaskToBack(false)

  

设备运行，如何屏蔽一直在顶层显示正在运行中 [https://blog.csdn.net/hanhan1016/article/details/121950582](https://blog.csdn.net/hanhan1016/article/details/121950582)

  

  

- **从** **PendingIntent** **启动** **activity** **时需要选择启用**

  

  

7:  

8:  小三星  
9:  HUAWEI Y9Prime 2019 V9 \ HUAWEI Mate 20 Pro V9

10:  米 9、红米

11:  moto g power

12:  oneplus \ galaxy s22 ultra 12 \ oppo findx3

13:  pixel 4

14:  pixel 7,  pixel 6 pro

  

**2****、竞品现状**

— airscreen

  

A. 10 以下手机，只要手机处于后台运行，则在接收到投屏信号时，直接打开 APP 到投屏页。

B. 10 及以上手机，如果用户没有授予Display over other apps 权限，则不能实现目标行为；若用户打开了该权限，则可以实现目标行为。

a.  权限的授予，需要应用层主动向用户索取，引导用户主动前往系统设置相关页授予。

b. 一旦用户授予后，在应用内打开或关闭 UI 开关是不起作用的。如果用户想要关闭，必须自行前往系统相关页面。竞品没有实现引导用户关闭的路径。

C. 测试出的 BUG

a. 在一些特殊的机子上，即使用户按照引导打开了权限，但依然不能在后台打开应用目标界面。如华为、小米、oppo 等

b. 测试机：米 9

D.  解决竞品 BUG 的方式

a. 打开后台弹出界面权限

b. 若想要在锁屏状态下，依然可以实现目标行为，则需要申请 show on lock screen 权限

  

———————————

非常有用的参考文档：

1、后台启动Activity的方案：[https://blog.csdn.net/wangjinyu501/article/details/132594354](https://blog.csdn.net/wangjinyu501/article/details/132594354) / https://juejin.cn/post/7273028821762703419

2、APP 保活，用户行为 [https://blog.csdn.net/weixin_35665584/article/details/117283130](https://blog.csdn.net/weixin_35665584/article/details/117283130)

3、官方限制后台启动 APP 文档 [https://developer.android.com/guide/components/activities/background-starts?hl=zh-cn](https://developer.android.com/guide/components/activities/background-starts?hl=zh-cn)

4、保活 

5、通知运行时权限 [https://developer.android.com/develop/ui/views/notifications/notification-permission?hl=zh-cn](https://developer.android.com/develop/ui/views/notifications/notification-permission?hl=zh-cn)

6、Android 实现App应用退到后台显示通知 [https://blog.csdn.net/ling9400/article/details/113611202](https://blog.csdn.net/ling9400/article/details/113611202)

7、[https://blog.csdn.net/zhangying1994/article/details/141217431](https://blog.csdn.net/zhangying1994/article/details/141217431)

8、[https://medium.com/make-android/start-android-activity-or-service-from-the-background-689f49167d80](https://medium.com/make-android/start-android-activity-or-service-from-the-background-689f49167d80) 从后台启动 activity 或 service

9、特殊机型 [https://github.com/huxq17/FloatBall/blob/master/app/src/main/java/com/huxq17/example/floatball/permission/rom/HuaweiUtils.java](https://github.com/huxq17/FloatBall/blob/master/app/src/main/java/com/huxq17/example/floatball/permission/rom/HuaweiUtils.java)

10、小米 [https://gist.github.com/olegcherr/dc79af4a0a82473e560e1b191a2bea02](https://gist.github.com/olegcherr/dc79af4a0a82473e560e1b191a2bea02)

11、后台执行限制 [https://developer.android.com/about/versions/oreo/background?hl=zh-cn](https://developer.android.com/about/versions/oreo/background?hl=zh-cn)

12、[https://blog.csdn.net/zssrxt/article/details/127470783](https://blog.csdn.net/zssrxt/article/details/127470783)  得出后台可以启动activity的情况如下 

  

  

  

  

  

temp

  

private fun startPendingActivity() {

    val options = ActivityOptions.makeBasic()._apply_ **{**

        _pendingIntentBackgroundActivityStartMode_ =

            ActivityOptions._MODE_BACKGROUND_ACTIVITY_START_ALLOWED_

    **}**

    PendingIntent.getActivity(this, 0,

        Intent(this, CastImageActivity::class._java_),

        PendingIntent._FLAG_IMMUTABLE_,

        options.toBundle()).send()

}

  

  

**后台启动** **APP - longquan** **项目应用**

  

**场景用例**

  

**1****、****APP** **处于后台，点击镜像，什么反应？**

a) 

**2****、****APP** **处于后台，点击镜像，发送端开始镜像，不断开，打开** **APP** **什么反应？**

a) 

**3****、****APP** **处于后台，点击镜像，发送端开始镜像，后断开，打开** **APP** **什么反应？**

a) 

**4****、****APP** **处于前台，点击镜像，什么反应？**

a)**  
5****、镜像过程中，网络断开，什么反应？**

a) 

  

  

  

**一、保活****APP**

1. 背景：在系统内存资源不足的情况下，会优先杀掉低优先级的进程。用户可感知的 app 往往不希望被系统所杀，系统默认service 是后台的，通过startForeground 把服务提到foreground 优先级，即成为可感知的级别。
2. 给用户主动选择后台运行的入口

3. override fun onBackPressed() {
4.     moveTaskToBack(false)
5. }  
    

6. CODE：

RunningAppService

class RunningAppService: Service() {

    private var notificationManager: NotificationManager? = null

    override fun onBind(p0: Intent?): IBinder? {

        return null

    }

  

    override fun onCreate() {

        super.onCreate()

        notificationManager = getSystemService(_NOTIFICATION_SERVICE_) as NotificationManager

    }

  

    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {

        createNotificationChannel()

        return super.onStartCommand(intent, flags, startId)

    }

  

    private fun createNotificationChannel() {

        if (Build.VERSION._SDK_INT_ >= Build.VERSION_CODES._O_) {

            notificationManager?.createNotificationChannel(

                NotificationChannel(

                    SamsungConstant.RUNNING_APP_CHANNEL_ID,

                    getString(R.string._app_name_) + SamsungConstant.RUNNING_APP_NOTIFICATION_NAME,

                    NotificationManager._IMPORTANCE_HIGH_

                )

            )

        }

        val builder = NotificationCompat.Builder(this, SamsungConstant.RUNNING_APP_CHANNEL_ID)

            .setPriority(NotificationCompat._PRIORITY_HIGH_)

            .setAutoCancel(false)

            .setOngoing(true)

            .setContentIntent(

                PendingIntent.getActivity(this, 9691,

                    Intent(this, CastImageActivity::class._java_),

                    PendingIntent._FLAG_UPDATE_CURRENT_ or PendingIntent._FLAG_IMMUTABLE_))

        if (Build.VERSION._SDK_INT_ >= Build.VERSION_CODES._Q_) {

            startForeground(SamsungConstant.RUNNING_APP_NOTIFY_ID, builder.build(),

                ServiceInfo._FOREGROUND_SERVICE_TYPE_CONNECTED_DEVICE_)

        } else {

            startForeground(SamsungConstant.RUNNING_APP_NOTIFY_ID, builder.build())

        }

    }

  

    override fun onDestroy() {

        super.onDestroy()

        ServiceCompat.stopForeground(this, ServiceCompat._STOP_FOREGROUND_REMOVE_)

    }

}  

manifest application层级

<service

    android:name=".cast.RunningAppService"

    android:foregroundServiceType="connectedDevice"

    android:exported="false"/>  

打开服务和移除服务

// 应用主界面的onscreate方法中调用

private fun showRunningAppNotification() {

    SamsungApplication.get()._let_ **{**

        ContextCompat.startForegroundService(**it**,

            Intent(**it**, RunningAppService::class._java_))

    **}**

}

  

// ondestory方法中调用

private fun stopRunningAppNotification() {

    SamsungApplication.get()._let_ **{** context **->**

        context.stopService(Intent(context, RunningAppService::class._java_))

    **}**

}  

1. 的

**二、后台启动** **APP**

1. android 10以下机型

2. 可以直接在后台启动应用某个activity。
3. 但如果在Activity上下文之外启动Activity会有限制，需要给intent添加flag：FLAG_ACTIVITY_NEW_TASK。
4. CODE

5. android 10以上机型

6. 官方开始限制应用从后台启动 APP 行为

7. https://developer.android.com/guide/components/activities/background-starts?hl=zh-cn
8. 目的：这些限制有助于最大限度地减少对用户的干扰， 让用户能够更好地控制其屏幕上显示的内容。

9. 解决方式

10. 请求必要权限 SYSTEM_ALERT_WINDOW，如果用户已向应用授予SYSTEM_ALERT_WINDOW权限，则可以在后台启动Activity。
11. 在 Android 10 Go 版本中，应用已经无法直接获得SYSTEM_ALERT_WINDOW权限。不过Android引入了一种称为"Display over other apps"（在其他应用上层显示）的新权限体系。这种新的权限体系允许应用请求"TYPE_APPLICATION_OVERLAY"类型的窗口权限。
12. 但在部分机型中，还需要请求“display pop-up windows while running in the background”权限，这种权限控制应用在后台是否允许弹出窗口。

13. CODE

14. 检查并请求"Display over other apps"窗口权限
15. // 处理在N - O 的某些手机上，Settings.canDrawOverlays(context) 总是返回 true 而实际上没有授权悬浮窗权限的情况
16. private fun canDrawOverlays(): Boolean {
17.     if (Build.VERSION._SDK_INT_ <= Build.VERSION_CODES._M_) {
18.         return true
19.     } else if (Build.VERSION._SDK_INT_ >= Build.VERSION_CODES._O_MR1_) {
20.         return Settings.canDrawOverlays(this)
21.     } else {
22.         if (Settings.canDrawOverlays(this)) {
23.             return true
24.         }
25.         try {
26.             // 如果为空，则返回 false
27.             this.getSystemService(Context._WINDOW_SERVICE_) ?: return false
28.             return true
29.         } catch (e: Exception) {
30.             e.printStackTrace()
31.         }
32.         return false
33.     }
34. }

35. // 请求引导用户开启权限
36. private fun requestDrawOverlays(): Boolean {
37.     if (canDrawOverlays()) {
38.         return true
39.     } else {
40.         val intent = Intent(Settings._ACTION_MANAGE_OVERLAY_PERMISSION_)
41.         intent.setData(Uri.parse("package:$_packageName_"))
42.         startActivityForResult(intent, 0)
43.         return false
44.     }
45. }  
    
46. 检查并请求“display pop-up windows while running in the background”窗口权限

47.   
    package com.boost.samsung.remote.utils

48. import android.annotation.SuppressLint
49. import android.app.AppOpsManager
50. import android.content.Context
51. import android.net.Uri
52. import android.os.Binder
53. import android.os.Build
54. import android.provider.Settings
55. import remote.common.utils.Logger

56. object PopBackgroundPermissionUtil {
57.     private const val TAG = "PopPermissionUtil"

58.     private const val HW_OP_CODE_POPUP_BACKGROUND_WINDOW = 100000
59.     private const val XM_OP_CODE_POPUP_BACKGROUND_WINDOW = 10021

60.     _/**_
61.     _*_ 是否有后台弹出页面权限
62.     _*/_
63.     fun hasPopupBackgroundPermission(context: Context): Boolean {
64.         if (isHuawei()) {
65.             return checkHwPermission(context)
66.         }
67.         if (isXiaoMi()) {
68.             return checkXmPermission(context)
69.         }
70.         if (isVivo()) {
71.             checkVivoPermission(context)
72.         }
73.         if (isOppo() && Build.VERSION._SDK_INT_ >= Build.VERSION_CODES._M_) {
74.             return Settings.canDrawOverlays(context)
75.         }
76.         return true
77.     }

78.     private fun isHuawei(): Boolean {
79.         return checkManufacturer("huawei")
80.     }

81.     private fun isXiaoMi(): Boolean {
82.         return checkManufacturer("xiaomi")
83.     }

84.     private fun isOppo(): Boolean {
85.         return checkManufacturer("oppo")
86.     }

87.     private fun isVivo(): Boolean {
88.         return checkManufacturer("vivo")
89.     }

90.     private fun checkManufacturer(manufacturer: String): Boolean {
91.         manufacturer._equals_(Build._MANUFACTURER_, true)._let_ **{** ans **->**
92.             Logger.d(TAG, "$manufacturer checkManufacturer: $ans")
93.             return ans
94.         **}**
95.     }

96.     private fun checkHwPermission(context: Context): Boolean {
97.         try {
98.             val c = Class.forName("com.huawei.android.app.AppOpsManagerEx")
99.             val m = c.getDeclaredMethod(
100.                 "checkHwOpNoThrow",
101.                 AppOpsManager::class._java_,
102.                 Int::class._javaPrimitiveType_,
103.                 Int::class._javaPrimitiveType_,
104.                 String::class._java_
105.             )
106.             val result = m.invoke(
107.                 c.newInstance(),
108.                 *_arrayOf_(
109.                     context.getSystemService(Context._APP_OPS_SERVICE_) as AppOpsManager,
110.                     HW_OP_CODE_POPUP_BACKGROUND_WINDOW,
111.                     Binder.getCallingUid(),
112.                     context._packageName_
113.                 )
114.             ) as Int
115.             Logger.d(TAG, "PopBackgroundPermissionUtil checkHwPermission result:" + (AppOpsManager._MODE_ALLOWED_ == result))
116.             return AppOpsManager._MODE_ALLOWED_ == result
117.         } catch (e: Exception) {
118.             //ignore
119.         }
120.         return true
121.     }

122.     private fun checkXmPermission(context: Context): Boolean {
123.         val ops = context.getSystemService(Context._APP_OPS_SERVICE_) as AppOpsManager
124.         try {
125.             val method = ops._javaClass_.getMethod(
126.                 "checkOpNoThrow", *_arrayOf_<Class<*>?>(
127.                     Int::class._javaPrimitiveType_, Int::class._javaPrimitiveType_, String::class._java_
128.                 )
129.             )

130.             val result = method.invoke(
131.                 ops,
132.                 XM_OP_CODE_POPUP_BACKGROUND_WINDOW,
133.                 android.os.Process.myUid(),
134.                 context._packageName_
135.             ) as Int
136.             Logger.d(TAG, "PopBackgroundPermissionUtil checkXmPermission result:" + (AppOpsManager._MODE_ALLOWED_ == result))
137.             return result == AppOpsManager._MODE_ALLOWED_
138.         } catch (e: Exception) {
139.             //ignore
140.         }
141.         return true
142.     }

143.     @SuppressLint("Range")
144.     private fun checkVivoPermission(context: Context): Boolean {
145.         val uri = Uri.parse("content://com.vivo.permissionmanager.provider.permission/start_bg_activity")
146.         val selection = "pkgname = ?"
147.         val selectionArgs = _arrayOf_(context._packageName_)
148.         var result = 1
149.         val contentResolver = context._contentResolver_
150.         try {
151.             contentResolver.query(uri, null, selection, selectionArgs, null)._use_ **{** cursor **->**
152.                 if (cursor?.moveToFirst() == true) {
153.                     result = cursor.getInt(cursor.getColumnIndex("currentstate"))
154.                 }
155.             **}**
156.         } catch (exception: Exception) {
157.             return true
158.         }
159.         Logger.d(TAG, "PopBackgroundPermissionUtil checkVivoPermission result:" + (AppOpsManager._MODE_ALLOWED_ == result))
160.         return result == AppOpsManager._MODE_ALLOWED_
161.     }
162. }  
      
    package com.boost.samsung.remote.utils

163. import android.app.Activity
164. import android.content.Context
165. import android.content.Intent
166. import android.content.pm.PackageManager
167. import android.net.Uri
168. import android.os.Build
169. import java.util.Locale
170. import android.provider.Settings
171. import remote.common.utils.Logger

172. class SystemAlertWindow(private val mSource: Activity) {

173.     fun start(requestCode: Int) {
174.         Logger.d(TAG, "SystemAlertWindow start")
175.         var intent: Intent?
176.         intent = if (MARK._contains_("huawei")) {
177.             huaweiApi(mSource)
178.         } else if (MARK._contains_("xiaomi")) {
179.             xiaomiApi(mSource)
180.         } else if (MARK._contains_("oppo")) {
181.             oppoApi(mSource)
182.         } else if (MARK._contains_("vivo")) {
183.             vivoApi(mSource)
184.         } else {
185.             if (Build.VERSION._SDK_INT_ >= Build.VERSION_CODES._M_) {
186.                 mDefaultApi(mSource)
187.             } else {
188.                 appDetailsApi(mSource)
189.             }
190.         }
191.         try {
192.             Logger.d(TAG, "startActivityForResult")
193.             mSource.startActivityForResult(intent, requestCode)
194.         } catch (e: Exception) {
195.             Logger.d(TAG, "catch Exception")
196.             intent = appDetailsApi(mSource)
197.             mSource.startActivityForResult(intent, requestCode)
198.         }
199.     }

200.     private fun huaweiApi(context: Context): Intent? {
201.         Logger.d(TAG, "huaweiApi")
202.         val intent = Intent()
203.         intent.setClassName(
204.             "com.huawei.systemmanager",
205.             "com.huawei.permissionmanager.ui.MainActivity"
206.         )
207.         if (hasActivity(context, intent)) {
208.             return intent
209.         }
210.         intent.setClassName(
211.             "com.huawei.systemmanager",
212.             "com.huawei.systemmanager.addviewmonitor.AddViewMonitorActivity"
213.         )
214.         if (hasActivity(context, intent)) {
215.             return intent
216.         }
217.         intent.setClassName(
218.             "com.huawei.systemmanager",
219.             "com.huawei.notificationmanager.ui.NotificationManagmentActivity"
220.         )
221.         return if (hasActivity(context, intent)) {
222.             intent
223.         } else mDefaultApi(context)
224.     }

225.     private fun xiaomiApi(context: Context): Intent? {
226.         Logger.d(TAG, "xiaomiApi")
227.         val intent = Intent("miui.intent.action.APP_PERM_EDITOR")
228.         intent.putExtra("extra_pkgname", context._packageName_)
229.         if (hasActivity(context, intent)) {
230.             return intent
231.         }
232.         intent.setClassName(
233.             "com.miui.securitycenter",
234.             "com.miui.permcenter.permissions.PermissionsEditorActivity"
235.         )
236.         if (hasActivity(context, intent)) {
237.             return intent
238.         }
239.         intent.setClassName(
240.             "com.miui.securitycenter",
241.             "com.miui.permcenter.permissions.AppPermissionsEditorActivity"
242.         )
243.         return if (hasActivity(context, intent)) {
244.             intent
245.         } else mDefaultApi(context)
246.     }

247.     private fun oppoApi(context: Context): Intent? {
248.         Logger.d(TAG, "oppoApi")
249.         val intent = Intent()
250.         intent.putExtra("packageName", context._packageName_)
251.         intent.setClassName(
252.             "com.color.safecenter",
253.             "com.color.safecenter.permission.floatwindow.FloatWindowListActivity"
254.         )
255.         if (hasActivity(context, intent)) {
256.             return intent
257.         }
258.         intent.setClassName(
259.             "com.coloros.safecenter",
260.             "com.coloros.safecenter.sysfloatwindow.FloatWindowListActivity"
261.         )
262.         if (hasActivity(context, intent)) {
263.             return intent
264.         }
265.         intent.setClassName("com.oppo.safe", "com.oppo.safe.permission.PermissionAppListActivity")
266.         return if (hasActivity(context, intent)) {
267.             intent
268.         } else mDefaultApi(context)
269.     }

270.     private fun vivoApi(context: Context): Intent? {
271.         Logger.d(TAG, "vivoApi")
272.         val intent = Intent()
273.         intent.setClassName(
274.             "com.iqoo.secure",
275.             "com.iqoo.secure.ui.phoneoptimize.FloatWindowManager"
276.         )
277.         intent.putExtra("packagename", context._packageName_)
278.         if (hasActivity(context, intent)) {
279.             return intent
280.         }
281.         intent.setClassName(
282.             "com.iqoo.secure",
283.             "com.iqoo.secure.safeguard.SoftPermissionDetailActivity"
284.         )
285.         return if (hasActivity(context, intent)) {
286.             intent
287.         } else mDefaultApi(context)
288.     }

289.     companion object {
290.         private val MARK = Build._MANUFACTURER_._lowercase_(Locale.getDefault())
291.         private const val TAG = "SystemAlertWindow"

292.         private fun appDetailsApi(context: Context): Intent {
293.             Logger.d(TAG, "appDetailsApi")
294.             val intent = Intent(Settings._ACTION_APPLICATION_DETAILS_SETTINGS_)
295.             intent._data_ = Uri.fromParts("package", context._packageName_, null)
296.             return intent
297.         }

298.         private fun mDefaultApi(context: Context): Intent? {
299.             Logger.d(TAG, "MdefaultApi")
300.             var intent: Intent? = null
301.             if (Build.VERSION._SDK_INT_ >= Build.VERSION_CODES._M_) {
302.                 intent = Intent(Settings._ACTION_MANAGE_OVERLAY_PERMISSION_)
303.             }
304.             intent?._data_ = Uri.fromParts("package", context._packageName_, null)
305.             return if (hasActivity(context, intent)) {
306.                 intent
307.             } else appDetailsApi(context)
308.         }

309.         private fun hasActivity(context: Context, intent: Intent?): Boolean {
310.             Logger.d(TAG, "hasActivity")
311.             val packageManager = context._packageManager_
312.             intent._let_ **{** mIntent **->**
313.                 if (mIntent == null) {
314.                     return false
315.                 } else {
316.                     return packageManager.queryIntentActivities(
317.                         mIntent,
318.                         PackageManager._MATCH_DEFAULT_ONLY_
319.                     ).size > 0
320.                 }
321.             **}**
322.         }
323.     }
324. }  
      
    private fun requestPopupInBackground(): Boolean {
325.     if (!PopBackgroundPermissionUtil.hasPopupBackgroundPermission(this)) {
326.         SystemAlertWindow(this).start(8880)
327.         return false
328.     } else {
329.         return true
330.     }
331. }  
    

332. USE

**//** **点击按钮开始** **timer** **十秒计时，到点打开指定****Activity**

**//** **这里的****timer****到点可替换成任何想要触发从后台打开****Activity****的时机**

**private fun startTimerToReopenApp() {**

    **timer.cancel()**

    **requestPopupInBackground().**_let_ **{**

        **if (it) {**

            **requestDrawOverlays().**_let_ **{ it2 ->**

                **if (it2) {**

                    **timer.start()**

                    **onBackPressed()**

                **}**

            **}**

        **}**

    **}**

**}**

  

**private val timer = object : CountDownTimer(10 * 1000, 1000) {**

    **override fun onTick(p0: Long) {**

        **Logger.d("TEST_REOPEN", "timer onTick $p0")**

    **}**

    **override fun onFinish() {**

        **Logger.d("TEST_REOPEN", "timer onFinish")**

        **reOpenApp()**

    **}**

**}**

**DONE**