Flutter

  

Google生的。

- 解决移动开发领域痛点：高性能、高保真 UI、开发性能三者不可兼得。

解决了吗？

  

UI 开发范式。

  

高刷渲染能力-高保真 UI。

  

Sky -> Flutter。 从一个实验项目落地，稳定版正式发布，可用于生产环境。

  

自身的痛点：

- 包体积。

- 自带渲染引擎。即使只是空项目，包体积也远大于原生应用。

  

  

分层架构

- 每一层都有其自己的角色和职责。
- “层”的存在，表明每一层只能与其上下层沟通。

  

单一数据源

- SSOT
- 通常保存在名为Repository的类中。

  

单向数据流

- UDF
- 将状态与显式该状态的 UI 解耦。

  

UI 是（不可变）状态的函数

- 状态改变 -> 触发依赖该状态的 UI 重建。

  

设计 Flutter 应用时，最重要的原则是关注点分离。

  

  

层

- 用户界面层
- （逻辑层）
- 数据层

  

架构模式

- MVVM

- Model, ViewModel, View
- 模型-视图-视图模型

  

  

  

是什么？

UI 框架。使用 Dart 语言。不依赖原生控件，自己用 Skia/Impeller 引擎把 UI 直接画在画布上。（自绘。）