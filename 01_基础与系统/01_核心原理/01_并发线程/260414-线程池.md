# 线程池
设计顶层思想：**关注点分离**。

为了解决高耦合问题。执行策略、任务定义、线程管理耦合实现。

## 本质作用
预先创建、可复用的一组线程集合，避免频繁创建销毁的开销。<br>
解耦任务提交和执行策略。<br>
控制并发度，防止无限制创建线程耗尽内存或导致频繁切换。

## 核心
基于 Executor 框架。

## 核心参数
核心线程数、最大线程数、空闲存活时间、任务队列（有界/无界）、拒绝策略。
